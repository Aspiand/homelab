name: database

services:
  chartdb:
    container_name: chartdb
    image: ghcr.io/chartdb/chartdb:latest
    restart: always
    networks:
      - cloudflare

networks:
  default:
  cloudflare:
    name: cloudflare_tunnel
    external: true
